//var webot = require('weixin-robot');
function loadWebot(app){
    //webot.watch(app, { token: app.get('wechat').token , path: '/wechat/users'});
    //webot.loads(['../controllers/wechat']);
};
exports.setup = loadWebot;
